//hoisting => variable and functions are hoisted which means their declaration is moved on the top the code
//its works on only var keyword not let
//undefined => not in existance
//not defined => exist but not defined
console.log('a', a);
var a = 12;
console.log('a', a);