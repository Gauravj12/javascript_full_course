//functions => we are writing some code who run particular logic and saved with the name for further use.
            // we are using functions mainly for 3 reasons
            // 1. when you don't wanna run your code immiediatly but in future
            // 2. when you want to reuse your code
            // 3. when you want to reuse your code with diffrent data


function fun1() { 
    console.log('hello')
}
fun1();

//with params (parameter) and arguments
// arguments => values which we are given for running the function
// parameter => variables in which we store the values

function fun2(a,b) { 
    console.log(a+b)
}
fun2(4,5);